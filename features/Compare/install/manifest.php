<?php
declare(strict_types=1);

// The unit \Nino\Features::activate() applies, add-only (see
// docs/features.md, "The Install Unit"). Compare ships no template, no route
// and no config default of its own - a project writes [compare ...] where a
// comparison belongs. All this unit carries is the three words a pair says,
// merged into text/<locale>.php for every available locale.
return [];
