<?php
declare(strict_types=1);

// The unit \Nino\Features::activate() applies, add-only (see
// docs/features.md, "The Install Unit"). Toc ships no template, no route and
// no config default of its own - a project writes [toc] where a list belongs.
// All this unit carries is the two words it says, merged into
// text/<locale>.php for every available locale.
return [];
