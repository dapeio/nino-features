<?php
declare(strict_types=1);

// The unit \Nino\Features::activate() applies, add-only (see
// docs/features.md, "The Install Unit"). Copy ships no template, no route and
// no config default of its own - a project writes [copy]...[/copy] around the
// thing worth copying. All this unit carries is the three words the button
// says, merged into text/<locale>.php for every available locale.
return [];
